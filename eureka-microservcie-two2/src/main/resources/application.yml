server:
  port: 4090
 
spring:
   application:
      name: driver-service-two 
  
eureka:
   client:
     fetch-registry: true
     register-with-eureka: true
     service-url:
       defaultZone: http://localhost:8761/eureka
    
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"